# TEMPORARILY DISABLED - Will be set up properly at a later stage
name: Daily Market Data Update (Disabled)

on:
  # Disabled - no triggers
  workflow_dispatch:
    inputs:
      enabled:
        description: 'This workflow is temporarily disabled'
        required: false
        default: false
        type: boolean

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false  # This ensures the job never runs
    steps:
      - name: Workflow Disabled
        run: |
          echo "This workflow is temporarily disabled"
          echo "Will be set up properly at a later stage"
